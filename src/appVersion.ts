// Single source of truth for the version badge shown across all SRT web parts.
// Keep this in sync with the "version" in config/package-solution.json on every release.
export const APP_VERSION = '1.0.105.0';
